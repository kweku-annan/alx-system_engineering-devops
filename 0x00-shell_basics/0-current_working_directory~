#!/bin/bash

current_directory=$(pwd) # Get the current working directory
absolute_path=$(realpath "$current_directory") # Get the absolute path of the current directory
echo "The absolute path of the current working directory is: $absolute_path"